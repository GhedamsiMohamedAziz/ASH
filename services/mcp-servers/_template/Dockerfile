# mcp-server-template — instructions.md §14.3 (N3 "à créer une fois")
# TODO(connector): rename the image/labels for the real connector once copied out of _template/.
FROM node:22-slim
WORKDIR /app
COPY package.json ./
COPY src ./src
CMD ["node", "src/server.ts"]
