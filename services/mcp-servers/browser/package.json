{
  "name": "@olma/mcp-browser",
  "version": "0.1.0",
  "description": "Axone Browser MCP server — anti-SSRF hardened (instructions.md §14)",
  "type": "module",
  "scripts": { "test": "node --test test/*.test.ts" },
  "engines": { "node": ">=23" }
}
