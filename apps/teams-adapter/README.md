# Teams Adapter

> **Status:** scaffold (Phase P2) · **Spec:** instructions.md §7.1

Normalizes Teams activities → InboundMessage; SSO, Adaptive Cards, proactive run results.

- **Stack:** Bot Framework SDK v4 + Azure Bot Service
- **Contracts:** see `packages/schemas` (InboundMessage / AgentEvent) and `packages/errors` (taxonomy §21).

_Implementation lands in roadmap phase P2 (§29)._
