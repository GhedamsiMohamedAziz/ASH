# Web App

> **Status:** scaffold (Phase P1) · **Spec:** instructions.md §4, §7.3

React chat UI: streaming, connectors, automations, admin. The only true token-by-token streaming channel.

- **Stack:** React 18 + TS + Vite, Tailwind, TanStack Query, Zustand, WebSocket/SSE
- **Contracts:** see `packages/schemas` (InboundMessage / AgentEvent) and `packages/errors` (taxonomy §21).

_Implementation lands in roadmap phase P1 (§29)._
